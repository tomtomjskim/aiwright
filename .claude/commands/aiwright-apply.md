Apply an aiwright recipe to generate AI prompt configuration. This automatically scores, profiles, and lints the prompt.

Run this command:
```bash
aiwright apply $ARGUMENTS
```

If no recipe name is provided, use "default".

The output shows: DNA code, quality score, lint results, and improvement tips.
If there are warnings or tips, explain what they mean and suggest fixes.
